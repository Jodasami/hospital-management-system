@model Sistema_Hospitales.Models.viewModels.mMedico
@{
    ViewBag.Title = "consultarMedicos";
    Layout = "~/Views/Shared/_Layout.cshtml";
}
<div class="container mt-5">
    <div class="row justify-content-center">
        <div class="col-md-6">
            <div class="card shadow-lg border-0">
                <div class="card-header bg-success text-white text-center">
                    <h3 class="mb-0">Detalles del Médico</h3>
                </div>
                <div class="card-body p-3">
                    <div class="d-flex justify-content-center">
                        <div class="table-responsive">
                            <table class="table table-borderless mx-auto">
                                <tbody>
                                    <tr>
                                        <th class="text-end pe-3">ID Médico:</th>
                                        <td>@Html.DisplayFor(model => model.IdMedico)</td>
                                    </tr>
                                    <tr>
                                        <th class="text-end pe-3">Nombre:</th>
                                        <td>@Html.DisplayFor(model => model.Nombre)</td>
                                    </tr>
                                    <tr>
                                        <th class="text-end pe-3">Especialidad:</th>
                                        <td>@Html.DisplayFor(model => model.Especialidad)</td>
                                    </tr>
                                    <tr>
                                        <th class="text-end pe-3">Teléfono:</th>
                                        <td>@Html.DisplayFor(model => model.Telefono)</td>
                                    </tr>
                                    <tr>
                                        <th class="text-end pe-3">Email:</th>
                                        <td>@Html.DisplayFor(model => model.Email)</td>
                                    </tr>
                                    <tr>
                                        <th class="text-end pe-3">Hospital:</th>
                                        <td>@Model.IdHospital - @ViewBag.NombreHospital</td>
                                    </tr>
                                </tbody>
                            </table>
                        </div>
                    </div>

                    <div class="text-center mt-4">
                        <input type="button" value="Volver" class="btn btn-secondary btn-lg"
                               onclick="document.location.href = '@Url.Content("~/Medico/mantMedicos")'" />
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>